/**
 * Функция — это мини-сценарий, который умеет много раз выполнять нужную нам однотипную логику.
 */
function printMessage() {
    console.log('Доброе утро! 🌻');

    // Остальная логика функции...
}

printMessage();
// console.log(printMessage.name);
