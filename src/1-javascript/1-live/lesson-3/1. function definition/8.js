/**
 * IIFE, или Immediately Invoked Function Expression — это мгновенно вызывающееся функциональное выражение.
 */

console.log('→ логика...');

(function() {
    const message = 'Доброе утро! 🌻';
    console.log(message);
})();

console.log('→ ещё логика...');
